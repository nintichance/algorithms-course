package percolation;

public class PercolationStats {
     //When p equals 0, the system does not percolate; when p equals 1, the system percolates
    //when p < p* a random n-by-n grid almost never percolates
    //when p > p*, a random n-by-n grid almost always percolates
}
